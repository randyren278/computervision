# test.py
import face_recognition

def test(image, model_dir, device_id):
    # Dummy implementation for the example
    # Replace with actual implementation

    # Load the model (dummy implementation)
    # In actual implementation, you will load and use the anti-spoofing model here

    # For now, we will assume the model always returns 1 (i.e., not a spoofer)
    return 1  # or some logic to return 1 or 0 based on anti-spoofing

# If you have a specific model and logic, replace the above with the actual implementation
